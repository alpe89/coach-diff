# =====================
# Maven Build Artifacts
# =====================
# The target/ folder contains everything Maven generates during build:
# - Compiled classes (.class)
# - Final JAR/WAR files
# - Test reports
# These files are regenerated with `mvn clean install`, so they don't belong in the repo.
target/
*.jar
*.war
*.ear
!.mvn/wrapper/maven-wrapper.jar

# Maven wrapper downloaded files
.mvn/wrapper/maven-wrapper.jar

# =====================
# IDE - IntelliJ IDEA
# =====================
# Each developer has their own IDE settings.
# Committing them causes merge conflicts on every pull.
.idea/
*.iml
*.ipr
*.iws
out/

# =====================
# IDE - VS Code
# =====================
.vscode/
*.code-workspace

# =====================
# IDE - Eclipse
# =====================
.project
.classpath
.settings/
bin/

# =====================
# Environment Variables
# =====================
# CRITICAL: Never commit files with real credentials!
# .env contains Riot Games and OpenAI API keys.
# Only commit .env.example as a template.
.env
.env.local
.env.*.local

# =====================
# Operating System
# =====================
# Hidden files created automatically by the OS.
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db
Desktop.ini

# =====================
# Logs
# =====================
# Logs are useful for debugging but don't belong in the repo.
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# =====================
# Node.js / React Native (Mobile)
# =====================
# node_modules/ is HUGE (hundreds of MB).
# It's regenerated with `npm install` by reading package.json.
node_modules/
.npm
.yarn/cache
.yarn/unplugged

# Expo (React Native framework)
.expo/
dist/
web-build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# Metro bundler cache
.metro-health-check*

# =====================
# Test Outputs
# =====================
test-output/
coverage/
.nyc_output/

# =====================
# Docker
# =====================
# Don't commit local Docker volumes
docker-volumes/

# =====================
# Misc
# =====================
*.swp
*.swo
*~
