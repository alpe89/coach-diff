# =============================================================================
# CoachDiff.ai - Environment Variables Template
# =============================================================================
#
# INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Replace placeholder values with your real credentials
# 3. NEVER commit the .env file (it's already in .gitignore)
#
# =============================================================================

# -----------------------------------------------------------------------------
# RIOT GAMES API
# -----------------------------------------------------------------------------
# To get an API key:
# 1. Go to https://developer.riotgames.com/
# 2. Log in with your Riot account
# 3. Generate a "Development API Key" (valid 24h, needs regeneration)
#
# NOTE: Dev keys have low rate limits (20 req/sec, 100 req/2min).
# For production, you need a "Production API Key" (requires Riot approval).

COACHDIFF_RIOT_API_KEY=RGAPI-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Your Riot ID (name#tag) to test the app.
# Example: if your ID is "PlayerOne#EUW", set:
#   COACHDIFF_RIOT_GAME_NAME=PlayerOne
#   COACHDIFF_RIOT_TAG_LINE=EUW
COACHDIFF_RIOT_GAME_NAME=YourSummonerName
COACHDIFF_RIOT_TAG_LINE=EUW

# Region/Platform of the game server.
# Common values:
#   - euw1 (Europe West)
#   - na1 (North America)
#   - kr (Korea)
#   - eun1 (Europe Nordic & East)
# Full list: https://developer.riotgames.com/docs/lol#routing-values
COACHDIFF_RIOT_REGION=euw1

# -----------------------------------------------------------------------------
# OPENAI API
# -----------------------------------------------------------------------------
# To get an API key:
# 1. Go to https://platform.openai.com/
# 2. Create an account and add credit ($5 is enough for development)
# 3. Go to API Keys and generate a new key
#
# We use gpt-4o-mini which costs ~$0.15/1M tokens (very cheap).
# Estimated MVP cost: $5-10 total.

COACHDIFF_OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# DATABASE (PostgreSQL)
# -----------------------------------------------------------------------------
# These values must match those in docker-compose.yml.
# For local development we use simple credentials.
# In production, use complex passwords managed by a secrets manager.

POSTGRES_DB=coachdiff
POSTGRES_USER=dev
POSTGRES_PASSWORD=dev

# -----------------------------------------------------------------------------
# REDIS (Cache)
# -----------------------------------------------------------------------------
# Redis doesn't require authentication in development.
# In production, configure password with REDIS_PASSWORD.

REDIS_HOST=localhost
REDIS_PORT=6379
